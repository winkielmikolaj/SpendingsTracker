@{
    ViewData["Title"] = "My Expenses";
}
@model List<Expense>
<head>
    <div>
        <h1 class="text-center">My Expenses</h1>
        <span>test pisania w asp.net mvc</span>
        <button>Test</button>
        <div>
            <a asp-action="Index" class="btn btn-primary">Overview</a>
        </div>
        <div>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Id
                        </th>
                        <th>
                            Value
                        </th>
                        <th>
                            Description
                        </th>
                        <th>
                                                        Actions
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        foreach (var expense in Model)
                        {
                            <tr>
                            <td>
                            @expense.Id
                            </td>
                                <td>
                                    @expense.Value
                                </td>
                                <td>
                                    @expense.Description
                                </td>
                                <td>
                                    <a asp-action="CreateEditExpense" asp-route-id="@expense.Id">Edit</a>
                                <a asp-action="Delete" asp-route-id="@expense.Id">Delete</a></td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</head>
